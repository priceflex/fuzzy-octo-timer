%h1 Projects

%table
  %tr
    %th Name
    %th Action
  -@projects.each do |project|
    %tr
      %td= project.name
      %td
        = link_to "Show Details", project
= link_to "Create New Project", new_project_path

